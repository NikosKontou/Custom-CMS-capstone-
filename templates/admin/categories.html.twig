<a href="create-category.php"><button>Insert</button></a>
<div class="row">
    <div class="col">Edit</div>
    <div class="col">Ordering</div>
    <div class="col">Visibility</div>

    <div class="col"></div>
</div>
{% for category in categories %}

    {#    send the GET to category.php so it can be retrieved by the database and display the correct category #}
    <div class="row">


        <div class="col"><a href="../admin/category-edit.php?id={{ category['id'] }}">{{ category['category_name'] }}</a> </div>
        <div class="col info">{{ category['ordering'] }}</div>
        <div class="col info">{{ category['visibility']==1?"visible":"hidden" }}</div>
        <div class="col deleteItem"><a href="../admin/categories.php?deleteId={{ category['id'] }}"><button>delete category</button></a> </div>
    </div>

{% endfor %}

